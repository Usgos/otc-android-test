# OTC Android Compatibility Test

This small APK only tests Android packaging of Kivy + Python + pyquotex,
websockets and HTTP dependencies. It does not log in or trade.

A successful build/install confirms the dependency stack is viable before
the full OTC Live AI application is packaged.
